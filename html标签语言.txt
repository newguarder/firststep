块级元素

    块级元素就是独占一行，无法与其他元素并列的元素标签。
    能接受宽高，不设置宽度 ，那么块级元素的宽就是父元素的100%
    整理的块级元素有下：h1-h6;div;ul;ol;form;p;table;

行级元素
    行级元素的宽高是不能设置的，默认的是字体的宽高。与其他的行内元素同存在一行内。
    行级元素的整理如下:基本所有的字体标签 b;strong;em;a;        input;sub;上标sup;下标;img;

重点标签的整理
    a标签








